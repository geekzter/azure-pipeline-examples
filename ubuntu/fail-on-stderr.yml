
pool:
  vmImage: 'ubuntu-latest'
steps:
- bash: |
    >&2 echo "error"

- task: Bash@3
  inputs:
    targetType: 'inline'
    script: ls -l doesnotexist
    failOnStderr: false
  condition: always()

- task: Shellpp@0
  inputs:
    type: 'InlineScript'
    script: ls -l doesnotexist
    failOnStderr: false
  condition: always()

- task: CmdLine@2
  inputs:
    script: ls -l doesnotexist
    failOnStderr: false
  condition: always()
  