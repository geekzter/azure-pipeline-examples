
pool:
  vmImage: 'ubuntu-latest'
steps:
- bash: |
    echo error >&2

- task: Bash@3
  inputs:
    targetType: 'inline'
    script: ls -l doesnotexist
  condition: always()

- task: Shellpp@0
  inputs:
    type: 'InlineScript'
    script: ls -l doesnotexist
  condition: always()

- task: CmdLine@2
  inputs:
    script: ls -l doesnotexist
  condition: always()
